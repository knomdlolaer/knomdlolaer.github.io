trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

steps:
- checkout: self
  persistCredentials: true

- script: |
    git config --global user.email "dilbagsb@gmail.com"
    git config --global user.name "knomdlolaer"
    echo "Pipeline is working!"
  displayName: 'Setup and verify pipeline'